{
  "name": "desktop-pet",
  "version": "1.0.0",
  "description": "Desktop pet — floating dog companion",
  "main": "main.js",
  "scripts": {
    "start": "ELECTRON_RUN_AS_NODE= npx electron . --disable-gpu-sandbox",
    "build": "npx electron-builder --win portable"
  },
  "devDependencies": {
    "electron": "^28.0.0"
  }
}
